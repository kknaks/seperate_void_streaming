version: "3.9"

services:
  server:
    image: void-streaming-demo:0.1.0
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      # spec-08 §4 기본값 — .env 에 명시되면 override
      SPEAKER_ENGINE_STORAGE_URL: ${SPEAKER_ENGINE_STORAGE_URL:-memory://}
    volumes:
      - hf_cache:/root/.cache/huggingface
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  hf_cache:
